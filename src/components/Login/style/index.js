import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        marginHorizontal: 12,
        flex: 1
    },

    input: {
        height: 40,
        //margin: 12,
        borderWidth: 1,
        padding: 10,
        borderColor: 'gray',
        borderRadius: 30

    }
});

export default styles;
